// Globals : These are variables which are accessible every where in the js file , there are provided by the run time like node 

// console.log(process);
// process is one of the globals provided by the runtime , process provides the information about the how the whole nodejs process is 
// getting executed and a lot of things we get acess to throught process global ( see the output of console.log(process))

// console.log(__dirname);
// This global is not always accessible 

console.log(module);